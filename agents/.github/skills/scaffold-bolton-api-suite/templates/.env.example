# API base URL (optional — defaults to the value in src/api/config/environment.ts)
# API_BASE_URL=__API_BASE_URL__

# If the API requires authentication, add the credential env vars here, e.g.:
# API_USER_EMAIL=
# API_USER_PASSWORD=
