export const STATUS_REGRA = ['Ativo', 'Inativo'] as const;

export const PRIORIDADE_REGRA = ['Alta', 'Média', 'Baixa'] as const;

export const REGRA_SORTABLE_FIELDS = ['nome', 'status', 'prioridade', 'createdAt', 'updatedAt'] as const;
